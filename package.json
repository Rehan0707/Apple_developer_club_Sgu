{
  "name": "apple-developer-club-sgu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"node --env-file-if-exists=.env server.js\" \"vite --host 127.0.0.1\"",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server.js",
    "preview": "node --env-file-if-exists=.env server.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.2.12"
  }
}
